export default class Cast {
    constructor({ id, movieId, description, userId }) {
      this.id = id;
      this.movieId = movieId;
      this.userId = userId;
      this.description = description;
    }
  }
  